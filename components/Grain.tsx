// Film grain over the whole page. Decorative, so hidden from readers.
// The look and the motion are in globals.css under Grain.
export default function Grain() {
  return <div aria-hidden="true" className="grain" />;
}
